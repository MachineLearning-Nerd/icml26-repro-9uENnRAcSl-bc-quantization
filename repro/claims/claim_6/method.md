# Claim 6 method

Enumerate binning RTVC pairs over four epsilon values, audit persistent
piecewise-quantizer jumps at four shrinking spacings, compare deterministic
TV=1 with the Gaussian stochastic TV criterion, and require a deliberately
misreported bin width to create violations.
